# Telegram bot token — get from @BotFather on Telegram (/newbot).
# Username must end in "bot".
TELEGRAM_BOT_TOKEN=123456:ABC-your-botfather-token

# Groq API key — https://console.groq.com/keys (free tier available).
# Used to call openai/gpt-oss-120b (or whichever AGENT_MODEL you pick).
GROQ_API_KEY=gsk-your-key

# Tavily API key — https://tavily.com (free tier available).
# Used for live web search (MOSPI / data.gov.in / census lookups, etc.).
TAVILY_API_KEY=tvly-your-key

# Public URL where /run.jsonl will be reachable AFTER deployment.
# This is literally what gets sent back to the grader in every answer,
# so it MUST point at a live, wget-able URL before the first graded run.
# Leave as the placeholder until you've deployed.
PUBLIC_LOG_URL=https://your-deployed-host.example.com/run.jsonl

# --- optional overrides (defaults shown) ------------------------------------

# Groq model ID. Defaults to openai/gpt-oss-120b (reasoning model).
# Other Groq-hosted options: llama-3.3-70b-versatile, llama-3.1-8b-instant,
# mixtral-8x7b-32768.
AGENT_MODEL=openai/gpt-oss-120b

# Groq's OpenAI-compatible endpoint. Override only if Groq publishes a new URL.
GROQ_BASE_URL=https://api.groq.com/openai/v1

# Reasoning models (gpt-oss-*) burn reasoning tokens against this budget.
# Groq free-tier TPM for openai/gpt-oss-120b is 8000 — input + this must fit.
# 4000 leaves headroom for system prompt + tool def + a few search rounds
# while still letting the model reason + answer. Bump if on a paid tier.
MAX_COMPLETION_TOKENS=4000

# Safety cap on tool-use rounds per question.
MAX_TOOL_ROUNDS=6

# Number of Tavily results returned per search.
TAVILY_MAX_RESULTS=5

# Where the JSONL run log gets written. The Dockerfile sets this to
# /app/logs/run.jsonl so the persistent disk on Render/Railway picks it up.
LOG_PATH=logs/run.jsonl